[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "deepsight"
version = "0.1.0"
description = "Device-native vision: describe images with the OS's own vision framework. No server, zero tokens."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [
    { name = "Reality Shifting Tech", email = "elias@realityshifting.tech" },
]
keywords = ["vision", "apple-vision", "ocr", "on-device", "no-server"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Operating System :: MacOS",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Software Development :: Libraries :: Python Modules",
]

dependencies = [
    "httpx>=0.27",
    "pillow>=10.4",
    "pydantic>=2.7",
    "pydantic-settings>=2.2",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.2",
    "pytest-asyncio>=0.23.7",
    "ruff>=0.5",
    "mypy>=1.10",
]

[project.scripts]
deepsight = "deepsight.__main__:main"

[project.urls]
Homepage = "https://github.com/Reality-Shifting-Tech/deepsight"
Repository = "https://github.com/Reality-Shifting-Tech/deepsight"
Source = "https://github.com/Reality-Shifting-Tech/deepsight"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.11"
files = ["src", "eval", "tests"]
warn_unused_ignores = true
disallow_untyped_defs = true
warn_redundant_casts = true
check_untyped_defs = true

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
